# MS Teams Support Tracker (Step 1)

Simple Node + Express + PostgreSQL app to log MS Teams support requests.

## Run locally

1. Install dependencies:

```bash
npm install


cp .env.example .env
# edit DATABASE_URL to match your Postgres

cp .env.example .env
# edit DATABASE_URL to match your Postgres

